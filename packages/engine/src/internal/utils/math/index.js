/**
 * Maths utils
 * @namespace Math
 */

import Smoothstep from './smoothstep';
import Clamp from "./clamp";
import { Ramdom, RandomFloor, RandomCeil, RandomRound } from "./random";
import Mix from './mix';
import Remap from './remap';
const math = {
    Smoothstep,
    Mix,
    Clamp,
    Ramdom,
    RandomFloor,
    RandomCeil,
    RandomRound,
};

export default math;
