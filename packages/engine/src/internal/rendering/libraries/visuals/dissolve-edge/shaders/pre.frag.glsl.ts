export default `
#ifndef DISSOLVE_EDGE

    #define DISSOLVE_EDGE

    varying vec3 vDissolveWorldPos;

    uniform float dissolveThreshold;
    uniform float dissolveEdgeWidth;
    uniform vec3 dissolveEdgeColor;

#endif
`;
